[project]
name = "dojo"
version = "0.1.0"
description = "Dojo — gamified battleground for Gittensor (Bittensor SN74) mining"
requires-python = ">=3.10"
dependencies = [
    "python-telegram-bot",
    "requests",
]

[project.optional-dependencies]
dev = ["pytest", "ruff"]
